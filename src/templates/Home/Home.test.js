describe("<Home/>", () => {
  test("should render the button with the text", () => {
    expect(1).toBe(1);
  });

  test("test two", () => {
    expect(1).toBe(1);
  });

  test("test three", () => {
    expect(1).toBe(1);
  });
});
